# Cloud Run runtime deps only. The bag generator / chart renderer (rosbags, numpy,
# Pillow, matplotlib) are NOT needed at runtime — the server only reads the already
# exported incident/ assets. Pinned to the versions the app was verified against.
starlette==1.0.0
uvicorn[standard]==0.42.0
httpx==0.28.1
