# 真实 rosbag 绝不进版本控制(脱敏边界,见 non_sensitive.md)
sample_data/
bag/
*.bag
*.db3
*.mcap

# 真 bag 仅供赛前人工理解 topic 形态;发布只允许合成 bag 及其导出资产

# Python 缓存
__pycache__/
*.pyc

# 多场景 eval 的中间产物(由 tools/eval_scenarios.py 确定性重建,不入库)
eval_build/
eval_incidents/

# Phase 2 private real-bag smoke-test outputs — local only, never published
private_eval/

# Private per-robot topic mapping (real topics / codes) — never committed
*.local.json

# Rerun .rrd export spike output (regenerable via tools/export_to_rerun.py)
rerun_build/
*.rrd

# Node build artifacts (Rerun web viewer tooling)
node_modules/

# No-build /rerun static assets, materialized by tools/prepare_rerun_web_assets.py (not committed)
backend/static/vendor/rerun-web-viewer/
backend/static/rerun/

# 密钥绝不入库
*.key
.gemini_key

# Internal / collaboration docs and parked experiments — kept locally, not published
plan.md
progress.md
launch_posts.md
data_pipeline.md
non_sensitive.md
demo.md
runbook.md
schema.md
submission.md
requirement.md
slide.md
*.pdf
backend/interactions_client.py
backend/interactions_check.py
backend/frontend/index_verified.html
*.mp4
*.mov
*.mkv
*.webm
*.MOV
*.MP4
*.MKV
*.WEBM
